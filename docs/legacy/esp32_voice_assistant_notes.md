# AI语音对话助手

## 1.实现ESP32和python后端的长时间连接

​	websocket  python后端长时间和ESP32通信，python发送ESP32回传

​	屏幕显示中间过程

## 2.ESP32语言录制

需要一定动画

## 3.ESP32发送音频文件至python后端

语言唤醒启动录制功能并录制语言发送至python后端

## 4.python把获取到的语言文件转换为文字

获取到的音频转换为文字并通过ollama调用deepseek模型进行对话

天气之类的通过共用api回传

## 5.ai生成的回答转换为语言文件发送至ESP32

播放语言同时发送文本信息通过屏幕显示并播放语言